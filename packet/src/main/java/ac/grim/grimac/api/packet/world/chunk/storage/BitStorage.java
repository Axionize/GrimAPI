package ac.grim.grimac.api.packet.world.chunk.storage;

public interface BitStorage extends BaseStorage {
    static BitStorage from(int bitsPerEntry, int size) {
        return null; // TODO (Packet Rewrite)
    }
}
