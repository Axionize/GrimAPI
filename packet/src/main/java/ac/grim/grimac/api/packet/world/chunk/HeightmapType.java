package ac.grim.grimac.api.packet.world.chunk;

import ac.grim.grimac.api.packet.types.Packet;

public interface HeightmapType {
    static HeightmapType read(Packet wrapper) {
        return null; // TODO (Packet Rewrite)
    }
}
